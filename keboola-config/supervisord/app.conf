# Pure static JavaScript application - no services needed
# Nginx serves static files directly from /app/static
# This file exists to satisfy the requirement of having at least one .conf file

# No programs to run - static files are served directly by Nginx
